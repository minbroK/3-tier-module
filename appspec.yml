version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/terraform/

hooks:
  AfterInstall:
    - location: /deploy_scripts/run_terraform.sh
      timeout: 300
      runas: root

//files의 소스와 afterinstall의location은 저장소에서의 해당 파일의 위치를 뜻한다.
